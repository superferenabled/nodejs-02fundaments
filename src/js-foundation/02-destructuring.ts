// console.log(process.env);

const { SHELL } = process.env;

// console.log(SHELL);

export const characters = [
  'flash',
  'aquaman',
  'green lantern',
  'batman',
  'superman',
];

const [f, a, batman] = characters;

// console.log(batman)
